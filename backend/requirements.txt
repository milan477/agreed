# Core (always required) — the platform runs end-to-end offline with just these.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
python-multipart>=0.0.9
httpx>=0.27

# LLM (text processing for chat/negotiation). With OPENAI_API_KEY set, the chat is
# fully LLM-driven; without it the platform falls back to the offline heuristics.
openai>=1.30

# Sponsor integrations (optional; enabled automatically when keys are present).
# Install with: pip install -e ".[full]"  — or uncomment as needed.
weave>=0.51
wandb>=0.17
# langgraph>=0.2
# dspy-ai>=2.4
# e2b-code-interpreter>=1.0
# exa-py>=1.0
# redis>=5.0
# mem0ai>=0.1
# sqlalchemy>=2.0
# psycopg[binary]>=3.1
# twilio>=9.0
